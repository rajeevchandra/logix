{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/mathmatrix run build",
  "outputDirectory": "artifacts/mathmatrix/dist/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
